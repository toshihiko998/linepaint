# linepaint (batch)

ラフ連番から「かっこいいアニメ線画」→「画材仕上げ（例：コピック風）」を2パスで生成する雛形です。

## 1. Install
```bash
python -m venv .venv
. .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
